[Unit]
Description=Setup zram swap
After=local-fs.target

[Service]
Type=oneshot
ExecStart=/bin/bash -c 'echo 1536M > /sys/block/zram0/disksize && mkswap /dev/zram0 && swapon -p 100 /dev/zram0'
RemainAfterExit=yes

[Install]
WantedBy=multi-user.target
